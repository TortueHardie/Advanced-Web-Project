{
  "name": "@advanced-web/prisma",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate --schema=prisma/schema.prisma",
    "migrate": "prisma migrate dev --schema=prisma/schema.prisma --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.6.0"
  },
  "devDependencies": {
    "prisma": "^5.6.0",
    "typescript": "^5.7.3"
  }
}
